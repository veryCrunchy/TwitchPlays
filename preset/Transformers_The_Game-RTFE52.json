{
  "CONFIG STILL IN PROGRESS": "WIP",
  "config": {
    "WINDOW": ["Dolphin", "RTFE52"],
    "TIMED_MODE": false,
    "TIMED_ON_INPUT": true,
    "INTERVAL": 2,
    "TIMEOUT": 0
  },
  "inputs": [
    {
      "name": "reset camera",
      "inputs": ["reset camera", "reset cam"],
      "outputs": ["Q"],
      "time": [0]
    },
    {
      "name": "reset camera",
      "inputs": ["reset camera", "reset cam"],
      "outputs": ["Q"],
      "time": [0]
    },
    {
      "name": "plus",
      "inputs": ["plus", "+", "pause"],
      "outputs": ["E"],
      "time": [0]
    },
    {
      "name": "back",
      "inputs": ["back", "b"],
      "outputs": ["B"],
      "time": [0]
    },
    {
      "name": "select",
      "inputs": ["select", "sel", "choose", "a"],
      "outputs": ["A"],
      "time": [0]
    },
    {
      "name": "select up",
      "inputs": ["select up", "sel up"],
      "outputs": ["Up"],
      "time": [0]
    },
    {
      "name": "select down",
      "inputs": ["select down", "sel down"],
      "outputs": ["Down"],
      "time": [0]
    },
    {
      "name": "select left",
      "inputs": ["select left", "sel left"],
      "outputs": ["Left"],
      "time": [0]
    },
    {
      "name": "select right",
      "inputs": ["select right", "sel right"],
      "outputs": ["Right"],
      "time": [0]
    }
  ]
}
